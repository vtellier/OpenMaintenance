---
description: Update ROADMAP.md after completed work
---

Read `ROADMAP.md` and understand its structure.

Then review what was accomplished this session (look at recent git diff, STATUS.md changes, and any new code written).

Update `ROADMAP.md`:
- Mark completed items from `- [ ]` to `- [x]`
- If new work was done that isn't captured by any existing checkbox, add it under the appropriate milestone
- Make sure the file stays consistent with `doc/` specifications — do NOT add items that contradict `doc/`

Read `doc/overview.md` and `doc/data-model.md` to verify alignment if you're unsure.
