import { html } from '@arrow-js/core'

export const DashboardPage = () =>
  html`<section class="page">
    <h1>Dashboard</h1>
    <p class="page__empty">Nothing due right now — you're all caught up.</p>
  </section>`
